module VM.Machine where

-- import Control.Monad.State

-- type Counter = State Int Int

-- tick :: Counter
-- tick = state (\x -> (x + 1, x + 1))

-- tick3 :: Counter
-- tick3 = do
--   tick
--   tick
--   tick

-- foomain :: IO ()
-- foomain = do
--   print (runState tick3 0)

